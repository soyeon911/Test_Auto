name: Crash Probe

on:
  workflow_dispatch:
    inputs:
      base_url:
        description: '서버 Base URL'
        required: false
        default: 'http://127.0.0.1:8080'
      probe_filter:
        description: 'pytest -k 필터 (예: enroll or match)'
        required: false
        default: ''

  # 매일 새벽 2시 KST (UTC 17:00) 자동 실행
  schedule:
    - cron: '0 17 * * *'

defaults:
  run:
    shell: powershell

env:
  BASE_URL:              ${{ inputs.base_url || 'http://127.0.0.1:8080' }}
  SERVER_DIR:            C:\Users\sypark5\TestAgent\qfe-api-server-no-license
  SERVER_EXE_NAME:       qfe-server.exe
  SERVER_LICENSE_KEY:    1
  SERVER_MODE_CHOICE:    1
  SERVER_INSTANCE_COUNT: 1
  SERVER_MODEL_PATH:     C:\Users\sypark5\TestAgent\qfe-api-server-no-license\model
  SERVER_DB_PATH:        C:\Users\sypark5\TestAgent\qfe-api-server-no-license\face_database.db
  SERVER_LOG_DIR:        server_logs
  PROBE_REPORT_DIR:      reports/crash_probe
  HEALTH_CHECK_PATH:     /health
  PYTHONIOENCODING:      utf-8

jobs:
  crash-probe:
    name: Server Crash Probe
    runs-on: [self-hosted, windows]
    timeout-minutes: 60

    steps:

      # ── 1. 체크아웃 ───────────────────────────────────────────────────────────
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      # ── 2. Python + 의존성 ────────────────────────────────────────────────────
      - name: Python 환경 설정
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: 의존성 설치
        run: |
          [Console]::OutputEncoding = [System.Text.Encoding]::UTF8
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # ── 3. 디렉터리 준비 ──────────────────────────────────────────────────────
      - name: 디렉터리 생성
        run: |
          New-Item -ItemType Directory -Path $env:SERVER_LOG_DIR   -Force | Out-Null
          New-Item -ItemType Directory -Path $env:PROBE_REPORT_DIR -Force | Out-Null

      # ── 4. 서버 기동 ──────────────────────────────────────────────────────────
      # stdin 파일 사전 작성 → Start-Process 파일 리다이렉션 → /health 폴링
      # ※ pipe(|) 방식은 서버가 프롬프트마다 대기하므로 무한 블록 발생
      - name: 서버 기동
        id: start_server
        run: |
          [Console]::OutputEncoding = [System.Text.Encoding]::UTF8
          $OutputEncoding = [System.Text.Encoding]::UTF8
          $PSDefaultParameterValues['Out-File:Encoding'] = 'utf8'

          $serverExe = Join-Path $env:SERVER_DIR $env:SERVER_EXE_NAME
          $stdinFile = Join-Path $env:SERVER_LOG_DIR "init_stdin.tmp"
          $stdoutLog = Join-Path $env:SERVER_LOG_DIR "server_stdout.log"
          $stderrLog = Join-Path $env:SERVER_LOG_DIR "server_stderr.log"
          $healthUrl = "$env:BASE_URL$env:HEALTH_CHECK_PATH"

          Remove-Item $stdoutLog, $stderrLog -Force -ErrorAction SilentlyContinue

          # 포트 8080 기존 점유 프로세스 정리
          netstat -ano -p tcp | Select-String ":8080\s" | ForEach-Object {
            if ($_.ToString() -match "LISTENING\s+(\d+)") {
              try { Stop-Process -Id ([int]$matches[1]) -Force -ErrorAction SilentlyContinue } catch {}
            }
          }
          Start-Sleep -Seconds 1

          # stdin 파일 사전 작성
          @(
            $env:SERVER_LICENSE_KEY,
            $env:SERVER_MODE_CHOICE,
            $env:SERVER_INSTANCE_COUNT,
            $env:SERVER_MODEL_PATH,
            $env:SERVER_DB_PATH,
            ""
          ) | Set-Content -Path $stdinFile -Encoding ascii

          # Start-Process + 파일 리다이렉션 (부모 PowerShell 수명과 독립)
          $proc = Start-Process `
            -FilePath         $serverExe `
            -WorkingDirectory $env:SERVER_DIR `
            -ArgumentList     "-host 0.0.0.0" `
            -RedirectStandardInput  $stdinFile `
            -RedirectStandardOutput $stdoutLog `
            -RedirectStandardError  $stderrLog `
            -PassThru `
            -WindowStyle Hidden

          Write-Host "[Server] PID=$($proc.Id)"
          "SERVER_PID=$($proc.Id)" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8

          # /health 폴링 (최대 30초)
          $ready = $false
          for ($i = 0; $i -lt 30; $i++) {
            Start-Sleep -Seconds 1
            if ($proc.HasExited) {
              Write-Host "[Server] 프로세스 조기 종료 (exit=$($proc.ExitCode))"
              break
            }
            try {
              $r = Invoke-WebRequest -Uri $healthUrl -UseBasicParsing -TimeoutSec 2
              if ($r.StatusCode -eq 200) { $ready = $true; break }
            } catch {}
          }

          if (-not $ready) {
            Write-Host "[Server] stdout:"; Get-Content $stdoutLog -ErrorAction SilentlyContinue
            Write-Host "[Server] stderr:"; Get-Content $stderrLog -ErrorAction SilentlyContinue
            throw "서버 기동 실패 - /health 응답 없음"
          }
          Write-Host "[Server] 준비 완료"

      # ── 5. Crash Probe 실행 ───────────────────────────────────────────────────
      - name: Crash Probe 실행
        id: crash_probe
        run: |
          [Console]::OutputEncoding = [System.Text.Encoding]::UTF8
          $OutputEncoding = [System.Text.Encoding]::UTF8
          $env:PYTHONIOENCODING = "utf-8"

          $reportFile = "$env:PROBE_REPORT_DIR\report.json"
          $healthUrl  = "$env:BASE_URL$env:HEALTH_CHECK_PATH"

          $pytestArgs = @(
            "-m", "pytest",
            "tests/manual/test_crash_probes.py",
            "--base-url", $env:BASE_URL,
            "--import-mode=importlib",
            "--json-report",
            "--json-report-file=$reportFile",
            "--tb=short",
            "-v",
            "-p", "no:randomly"
          )

          $filter = "${{ inputs.probe_filter }}"
          if ($filter) { $pytestArgs += @("-k", $filter) }

          & python @pytestArgs 2>&1 | Tee-Object -FilePath "$env:PROBE_REPORT_DIR\pytest_output.txt"
          $exitCode = $LASTEXITCODE
          Write-Host "[Probe] pytest 종료코드: $exitCode"

          # 서버 생존 확인
          $alive = $false
          try {
            $r = Invoke-WebRequest -Uri $healthUrl -UseBasicParsing -TimeoutSec 3
            $alive = ($r.StatusCode -eq 200)
          } catch {}

          $crashCount = if ($alive) { 0 } else { 1 }
          $probeExit  = if ($exitCode -eq 0) { "0" } else { "1" }

          Write-Host "[Probe] server_alive=$alive  crash_count=$crashCount"
          "crash_count=$crashCount" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
          "probe_exit=$probeExit"   | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8

      # ── 6. 결과 CSV 생성 ──────────────────────────────────────────────────────
      # Python 코드를 YAML here-string에 직접 쓰면 들여쓰기 충돌 발생
      # → scripts/gen_probe_csv.py 로 분리하여 단순 호출
      - name: 결과 CSV 리포트 생성
        if: always()
        run: |
          [Console]::OutputEncoding = [System.Text.Encoding]::UTF8
          $env:PYTHONIOENCODING = "utf-8"
          python scripts/gen_probe_csv.py

      # ── 7. 아티팩트 업로드 ────────────────────────────────────────────────────
      - name: 리포트 업로드
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: crash-probe-reports-${{ github.run_id }}
          path: ${{ env.PROBE_REPORT_DIR }}/
          retention-days: 30

      - name: 서버 로그 업로드
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: crash-probe-server-logs-${{ github.run_id }}
          path: ${{ env.SERVER_LOG_DIR }}/
          retention-days: 30

      # ── 8. 최종 판정 ──────────────────────────────────────────────────────────
      - name: 최종 판정
        if: always()
        run: |
          [Console]::OutputEncoding = [System.Text.Encoding]::UTF8

          $crashCount = "${{ steps.crash_probe.outputs.crash_count }}"
          $probeExit  = "${{ steps.crash_probe.outputs.probe_exit }}"

          Write-Host "[Result] crash_count=$crashCount  probe_exit=$probeExit"

          if ([string]::IsNullOrWhiteSpace($crashCount)) {
            Write-Host "::error::크래시 카운트 정보 없음 (probe 단계 실패)"
            exit 1
          }
          if ([int]$crashCount -gt 0) {
            Write-Host "::error::서버 크래시 감지 - CGO 호출 전 입력 검증 필요"
            exit 2
          }
          if ($probeExit -ne "0") {
            Write-Host "::warning::VALIDATION_GAP 또는 테스트 실패 감지"
            exit 1
          }
          Write-Host "[Result] 모든 프로브 통과"

      # ── 9. 서버 종료 (항상) ───────────────────────────────────────────────────
      - name: 서버 종료
        if: always()
        run: |
          [Console]::OutputEncoding = [System.Text.Encoding]::UTF8

          if ($env:SERVER_PID) {
            try {
              Stop-Process -Id ([int]$env:SERVER_PID) -Force -ErrorAction SilentlyContinue
              Write-Host "[Cleanup] PID=$env:SERVER_PID 종료"
            } catch {}
          }

          # 포트에 남아있는 프로세스 강제 정리
          netstat -ano -p tcp | Select-String ":8080\s" | ForEach-Object {
            if ($_.ToString() -match "LISTENING\s+(\d+)") {
              try { Stop-Process -Id ([int]$matches[1]) -Force -ErrorAction SilentlyContinue } catch {}
            }
          }
