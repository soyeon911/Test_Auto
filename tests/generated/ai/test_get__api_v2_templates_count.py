# Auto-generated by TCGeneratorAgent [ai]
# GET /api/v2/templates-count
# operation : get_/api/v2/templates-count
# spec_hash : e612e721815a396633096167fe25f6b6c51bc211a276bd2c4f9a4755980596aa
# ───────────────────────────────────────────────────────
import pytest
import requests

import requests
import pytest
import json

def test_combinatorial_negatives():
    base_url = "http://example.com/api/v2/enroll"
    params = {"user_id": -1, "template_id": 0}
    resp = requests.get(f"{base_url}", params=params, timeout=10)
    body = resp.json()
    assert body.get("success") == False or body.get("error_code", 0) < 0, (
        f"Expected error but got: {resp.text[:300]}"
    )

def test_domain_logic_duplicate_enroll():
    base_url = "http://example.com/api/v2/enroll"
    user_id = 123
    resp = requests.post(f"{base_url}", json={"user_id": user_id, "template_id": None}, timeout=10)
    body = resp.json()
    assert body.get("success") == False or body.get("error_code", 0) < 0, (
        f"Expected error but got: {resp.text[:300]}"
    )

def test_injection_fuzzing_null_bytes():
    base_url = "http://example.com/api/v2/enroll"
    user_id = 123
    template_id = 456