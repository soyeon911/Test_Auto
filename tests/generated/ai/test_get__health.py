# Auto-generated by TCGeneratorAgent [ai]
# GET /health
# operation : get_/health
# spec_hash : da80c8d175837e22a2cf7b3718ec30f6724074e259a101e9b0fc1bc895495278
# ───────────────────────────────────────────────────────
import pytest
import requests

import requests

def test_get__enroll_negative_multiple_fields():
    base_url = "http://localhost:8080/get_enroll"
    params = {
        "user_id": -1,
        "face_image": "",
        "name": ""
    }
    resp = requests.post(f"{base_url}", json=params, timeout=10)
    body = resp.json()
    assert body.get("success") == False or body.get("error_code", 0) < 0, (
        f"Expected error but got: {resp.text[:300]}"
    )

def test_get__identify_domain_error_unknown_user_id():
    base_url = "http://localhost:8080/get_identify"
    params = {
        "user_id": "abc123",
        "face_image": "",
        "name": ""
    }
    resp = requests.post(f"{base_url}", json=params, timeout=10)
    body = resp.json()
    assert body.get("success") == False or body.get("error_code", 0) < 0, (
        f"Expected error but got: {resp.text[:300]}"
    )

import requests

def test_get__identify_domain_error_duplicate_enroll():
    base_url