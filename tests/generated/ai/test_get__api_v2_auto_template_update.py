# Auto-generated by TCGeneratorAgent [ai]
# GET /api/v2/auto-template-update
# operation : get_/api/v2/auto-template-update
# spec_hash : 9bff54a585ec2a63bfaf6048fbde6f081c69b0923fa7a20cd21f344baaf8a74b
# ───────────────────────────────────────────────────────
import pytest
import requests

import requests
import pytest

def test_combinational_negatives_enroll_duplicate(base_url):

    # Test duplicate user_id
    data = {"user_id": 123, "base64_image": "invalid_base64", "confidence_threshold": 0.9}
    resp = requests.post(f"{base_url}/api/v2/enroll", json=data, timeout=10)
    body = resp.json()
    assert body.get("success") == False and body.get("error_code") < 0, (
        f"Expected error but got: {resp.text[:300]}"
    )

    # Test missing required fields
    data = {"user_id": "123", "base64_image": "", "confidence_threshold": None}
    resp = requests.post(f"{base_url}/api/v2/enroll", json=data, timeout=10)
    body = resp.json()
    assert body.get("success") == False and body.get("error_code") < 0, (
        f"Expected error but got: {resp.text[:300]}"
    )

def test_combinational_negatives_identify_unknown_user_id(base_url):
    base_url