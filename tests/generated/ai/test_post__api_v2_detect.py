# Auto-generated by TCGeneratorAgent [ai]
# POST /api/v2/detect
# operation : post_/api/v2/detect
# spec_hash : 1e8c121f800ffc96a0217ee8fb0e8d38ef6d6f8b3fce1a6fca51f05c5fe86a28
# ───────────────────────────────────────────────────────
import pytest
import requests

def test_post__api_v2_detect_combinatorial_negative():
    base_url = "http://example.com/api/v2/detect"
    image_data = {
        "success": False,
        "error_code": -1,
        "msg": "Missing required field 'user_id'"
    }
    params = {"user_id": None}
    resp = requests.post(f"{base_url}/", json=image_data, timeout=10)
    body = resp.json()
    assert body.get("success") == False and body.get("error_code", 0) < 0, (
        f"Expected error but got: {resp.text[:300]}"
    )
    assert len(resp.json().get("msg")) > 50, "Error message not long enough"


def test_post__api_v2_detect_domain_logic_duplicate_enroll():
    base_url = "http://example.com/api/v2/detect"
    image_data = {
        "success": True,
        "data": {"user_id": 1}
    }
    params = {"user_id": 1, "enrollment_id": 1}  # Duplicate enrollment
    resp = requests.post(f"{base_url}/", json=image_data, timeout=10)
    body