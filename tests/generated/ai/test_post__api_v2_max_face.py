# Auto-generated by TCGeneratorAgent [ai]
# POST /api/v2/max-face
# operation : post_/api/v2/max-face
# spec_hash : de476c93f5dcf4296eed022f03a910d1e4e78add25595c4bb92cc27daab9a199
# ───────────────────────────────────────────────────────
import pytest
import requests

def test_post__api_v2_max_face_combinatorial_negatives():
    base_url = "https://example.com"
    resp = requests.post(f"{base_url}/api/v2/max-face", 
                        json={"max_face": -1, "other_field": "test"}, 
                        timeout=10)
    body = resp.json()
    assert body.get("success") == False or body.get("error_code", 0) < 0, (
        f"Expected error but got: {resp.text[:300]}"
    )

def test_post__api_v2_max_face_injection_fuzzing():
    import base64
    import random

    base_url = "https://example.com"
    length = 10000  # 10 KB
    for _ in range(5):
        image_bytes = b'x' * length
        image_str = base64.b64encode(image_bytes).decode('utf-8')
        resp = requests.post(f"{base_url}/api/v2/max-face", 
                            json={"max_face": random.randint(-1, 100), "image": image_str}, 
                            timeout=10)
        body = resp.json()
        assert body.get("success") == False or body