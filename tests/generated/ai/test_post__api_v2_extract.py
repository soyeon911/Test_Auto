# Auto-generated by TCGeneratorAgent [ai]
# POST /api/v2/extract
# operation : post_/api/v2/extract
# spec_hash : ef95daea5a5cb84594bbafb92969624b3d0646909bb94216eb1c81f6fea78882
# ───────────────────────────────────────────────────────
import pytest
import requests

import requests
import pytest

def test_post__api_v2_extract_combinant_negative_fields():
    base_url = "http://localhost:8080"
    path = "/api/v2/extract"

    # Combinatorial negatives: 3 bad fields simultaneously
    data = {
        "image_data": "",
        "user_id": -1,
        "name": None
    }
    resp = requests.post(f"{base_url}{path}", json=data, timeout=10)
    body = resp.json()
    assert body.get("success") == False or body.get("error_code", 0) < 0, (
        f"Expected error but got: {resp.text[:300]}"
    )


def test_post__api_v2_extract_domain_logic_error_duplicate_enroll():
    base_url = "http://localhost:8080"
    path = "/api/v2/extract"

    # Domain / business logic: duplicate enroll
    data = {
        "image_data": "",
        "user_id": 1,
        "name": "John"
    }
    resp = requests.post(f"{base_url}{path}", json=data, timeout=10)
    body = resp.json()
    assert body.get("success") == False