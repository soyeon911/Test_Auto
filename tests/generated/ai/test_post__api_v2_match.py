# Auto-generated by TCGeneratorAgent [ai]
# POST /api/v2/match
# operation : post_/api/v2/match
# spec_hash : b04a795343d6b987ad053952b9c0ec331696d45b1a678951b6c991c9934f166c
# ───────────────────────────────────────────────────────
import pytest
import requests

def test_combinatorial_negatives():
    base_url = "http://localhost:8080"
    resp = requests.post(f"{base_url}/api/v2/match", json={
        "template1": "",
        "template2": ""
    }, timeout=10)
    body = resp.json()
    assert body.get("success") == False, (
        f"Expected error but got: {resp.text[:300]}"
    )

def test_injection_fuzzing():
    base_url = "http://localhost:8080"
    resp = requests.post(f"{base_url}/api/v2/match", json={
        "template1": "<script>alert('xss')</script>",
        "template2": ""
    }, timeout=10)
    body = resp.json()
    assert body.get("success") == False, (
        f"Expected error but got: {resp.text[:300]}"
    )