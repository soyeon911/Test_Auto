# Auto-generated by TCGeneratorAgent [rule]
# POST /api/v2/extract
# operation : post_/api/v2/extract
# spec_hash : ef95daea5a5cb84594bbafb92969624b3d0646909bb94216eb1c81f6fea78882
# ───────────────────────────────────────────────────────
import pytest
import requests

def test_post__api_v2_extract_positive(base_url):
    """[rule:positive] Happy-path — valid request should succeed."""
    resp = requests.post(f"{base_url}/api/v2/extract", json={'image_data': 'iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNk+M9QDwADhgGAWjR9awAAAABJRU5ErkJggg=='}, timeout=10)
    assert resp.status_code in [200], (
        f"Expected success, got {resp.status_code}: {resp.text[:200]}"
    )



def test_post__api_v2_extract_semantic_image_data_invalid_b64(base_url):
    """[rule:semantic_probe] 'image_data' tag=base64_image probe=invalid_b64."""
    resp = requests.post(f"{base_url}/api/v2/extract", json={'image_data': 'not_base64!@#'}, timeout=10)
    assert resp.status_code < 500, (
        f"[FAIL] semantic:invalid_b64 on 'image_data' — server crashed\n"
        f"  Status : {resp.status_code}\n"
        f"  Body   : {resp.text[:300]}"
    )
    body_json = resp.json()
    assert body_json.get("success") == False or body_json.get("error_code", 0) < 0, (
        f"[FAIL] semantic:invalid_b64 on 'image_data' — expected error response\n"
        f"  success    : {body_json.get('success')}\n"
        f"  error_code : {body_json.get('error_code')}\n"
        f"  msg        : {body_json.get('msg')}\n"
        f"  Full body  : {resp.text[:300]}"
    )




def test_post__api_v2_extract_semantic_image_data_empty_b64(base_url):
    """[rule:semantic_probe] 'image_data' tag=base64_image probe=empty_b64."""
    resp = requests.post(f"{base_url}/api/v2/extract", json={'image_data': ''}, timeout=10)
    assert resp.status_code < 500, (
        f"[FAIL] semantic:empty_b64 on 'image_data' — server crashed\n"
        f"  Status : {resp.status_code}\n"
        f"  Body   : {resp.text[:300]}"
    )
    body_json = resp.json()
    assert body_json.get("success") == False or body_json.get("error_code", 0) < 0, (
        f"[FAIL] semantic:empty_b64 on 'image_data' — expected error response\n"
        f"  success    : {body_json.get('success')}\n"
        f"  error_code : {body_json.get('error_code')}\n"
        f"  msg        : {body_json.get('msg')}\n"
        f"  Full body  : {resp.text[:300]}"
    )

