# Auto-generated by TCGeneratorAgent [rule]
# GET /health
# operation : get_/health
# spec_hash : da80c8d175837e22a2cf7b3718ec30f6724074e259a101e9b0fc1bc895495278
# ───────────────────────────────────────────────────────
import pytest
import requests

def test_get__health_positive(base_url):
    """[rule:positive] Happy-path — valid request should succeed."""
    resp = requests.get(f"{base_url}/health", timeout=10)
    assert resp.status_code in [200], (
        f"Expected success, got {resp.status_code}: {resp.text[:200]}"
    )
