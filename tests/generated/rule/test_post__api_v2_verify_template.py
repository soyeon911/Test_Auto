# Auto-generated by TCGeneratorAgent [rule]
# POST /api/v2/verify-template
# operation : post_/api/v2/verify-template
# spec_hash : 38be28430c31204f5272185a1157bf3d9b6bcca05e38e9603182bd648691d8a8
# ───────────────────────────────────────────────────────
import pytest
import requests

def test_post__api_v2_verify_template_positive(base_url):
    """[rule:positive] Happy-path — valid request should succeed."""
    resp = requests.post(f"{base_url}/api/v2/verify-template", json={'template_data': 'AAEC', 'user_id': 1}, timeout=10)
    assert resp.status_code in [200], (
        f"Expected success, got {resp.status_code}: {resp.text[:200]}"
    )



def test_post__api_v2_verify_template_missing_body_template_data(base_url):
    """[rule:missing_required] Omit required body field 'template_data' → error response."""
    resp = requests.post(f"{base_url}/api/v2/verify-template", json={'user_id': 1}, timeout=10)
    assert resp.status_code == 200, (
        f"[FAIL] missing body field 'template_data' — unexpected HTTP status\n"
        f"  Status : {resp.status_code}\n"
        f"  Body   : {resp.text[:300]}"
    )
    body = resp.json()
    assert body.get("success") == False or body.get("error_code", 0) < 0, (
        f"[FAIL] missing body field 'template_data' — expected error response\n"
        f"  success    : {body.get('success')}\n"
        f"  error_code : {body.get('error_code')}\n"
        f"  msg        : {body.get('msg')}\n"
        f"  Full body  : {resp.text[:300]}"
    )




def test_post__api_v2_verify_template_missing_body_user_id(base_url):
    """[rule:missing_required] Omit required body field 'user_id' → error response."""
    resp = requests.post(f"{base_url}/api/v2/verify-template", json={'template_data': 'AAEC'}, timeout=10)
    assert resp.status_code == 200, (
        f"[FAIL] missing body field 'user_id' — unexpected HTTP status\n"
        f"  Status : {resp.status_code}\n"
        f"  Body   : {resp.text[:300]}"
    )
    body = resp.json()
    assert body.get("success") == False or body.get("error_code", 0) < 0, (
        f"[FAIL] missing body field 'user_id' — expected error response\n"
        f"  success    : {body.get('success')}\n"
        f"  error_code : {body.get('error_code')}\n"
        f"  msg        : {body.get('msg')}\n"
        f"  Full body  : {resp.text[:300]}"
    )




def test_post__api_v2_verify_template_wrong_type_body_user_id(base_url):
    """[rule:wrong_type] Pass wrong type for body field 'user_id' (expected integer) → error response."""
    resp = requests.post(f"{base_url}/api/v2/verify-template", json={'template_data': 'AAEC', 'user_id': '"not_an_integer"'}, timeout=10)
    assert resp.status_code < 500, (
        f"[FAIL] wrong type for body field 'user_id' — server crashed\n"
        f"  Status : {resp.status_code}\n"
        f"  Body   : {resp.text[:300]}"
    )
    body = resp.json()
    assert body.get("success") == False or body.get("error_code", 0) < 0, (
        f"[FAIL] wrong type for body field 'user_id' — expected error response\n"
        f"  success    : {body.get('success')}\n"
        f"  error_code : {body.get('error_code')}\n"
        f"  msg        : {body.get('msg')}\n"
        f"  Full body  : {resp.text[:300]}"
    )




def test_post__api_v2_verify_template_semantic_template_data_invalid_b64(base_url):
    """[rule:semantic_probe] 'template_data' tag=base64_template probe=invalid_b64."""
    resp = requests.post(f"{base_url}/api/v2/verify-template", json={'template_data': 'not_base64!@#', 'user_id': 1}, timeout=10)
    assert resp.status_code < 500, (
        f"[FAIL] semantic:invalid_b64 on 'template_data' — server crashed\n"
        f"  Status : {resp.status_code}\n"
        f"  Body   : {resp.text[:300]}"
    )
    body_json = resp.json()
    assert body_json.get("success") == False or body_json.get("error_code", 0) < 0, (
        f"[FAIL] semantic:invalid_b64 on 'template_data' — expected error response\n"
        f"  success    : {body_json.get('success')}\n"
        f"  error_code : {body_json.get('error_code')}\n"
        f"  msg        : {body_json.get('msg')}\n"
        f"  Full body  : {resp.text[:300]}"
    )




def test_post__api_v2_verify_template_semantic_template_data_empty_b64(base_url):
    """[rule:semantic_probe] 'template_data' tag=base64_template probe=empty_b64."""
    resp = requests.post(f"{base_url}/api/v2/verify-template", json={'template_data': '', 'user_id': 1}, timeout=10)
    assert resp.status_code < 500, (
        f"[FAIL] semantic:empty_b64 on 'template_data' — server crashed\n"
        f"  Status : {resp.status_code}\n"
        f"  Body   : {resp.text[:300]}"
    )
    body_json = resp.json()
    assert body_json.get("success") == False or body_json.get("error_code", 0) < 0, (
        f"[FAIL] semantic:empty_b64 on 'template_data' — expected error response\n"
        f"  success    : {body_json.get('success')}\n"
        f"  error_code : {body_json.get('error_code')}\n"
        f"  msg        : {body_json.get('msg')}\n"
        f"  Full body  : {resp.text[:300]}"
    )




def test_post__api_v2_verify_template_semantic_user_id_negative_id(base_url):
    """[rule:semantic_probe] 'user_id' tag=numeric_id probe=negative_id."""
    resp = requests.post(f"{base_url}/api/v2/verify-template", json={'template_data': 'AAEC', 'user_id': -1}, timeout=10)
    assert resp.status_code < 500, (
        f"[FAIL] semantic:negative_id on 'user_id' — server crashed\n"
        f"  Status : {resp.status_code}\n"
        f"  Body   : {resp.text[:300]}"
    )
    body_json = resp.json()
    assert body_json.get("success") == False or body_json.get("error_code", 0) < 0, (
        f"[FAIL] semantic:negative_id on 'user_id' — expected error response\n"
        f"  success    : {body_json.get('success')}\n"
        f"  error_code : {body_json.get('error_code')}\n"
        f"  msg        : {body_json.get('msg')}\n"
        f"  Full body  : {resp.text[:300]}"
    )




def test_post__api_v2_verify_template_semantic_user_id_zero_id(base_url):
    """[rule:semantic_probe] 'user_id' tag=numeric_id probe=zero_id."""
    resp = requests.post(f"{base_url}/api/v2/verify-template", json={'template_data': 'AAEC', 'user_id': 0}, timeout=10)
    assert resp.status_code < 500, (
        f"[FAIL] semantic:zero_id on 'user_id' — server crashed\n"
        f"  Status : {resp.status_code}\n"
        f"  Body   : {resp.text[:300]}"
    )
    body_json = resp.json()
    assert body_json.get("success") == False or body_json.get("error_code", 0) < 0, (
        f"[FAIL] semantic:zero_id on 'user_id' — expected error response\n"
        f"  success    : {body_json.get('success')}\n"
        f"  error_code : {body_json.get('error_code')}\n"
        f"  msg        : {body_json.get('msg')}\n"
        f"  Full body  : {resp.text[:300]}"
    )

