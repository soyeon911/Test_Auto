# Auto-generated by TCGeneratorAgent [rule]
# POST /api/v2/match-images
# operation : post_/api/v2/match-images
# spec_hash : b902bcce6f974e0ddd430a2f055c0c64ce4232454a811b421ca18e062c0b5871
# ───────────────────────────────────────────────────────
import pytest
import requests

def test_post__api_v2_match_images_positive(base_url):
    """[rule:positive] Happy-path — valid request should succeed."""
    resp = requests.post(f"{base_url}/api/v2/match-images", json={'image1_data': 'iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNk+M9QDwADhgGAWjR9awAAAABJRU5ErkJggg==', 'image2_data': 'iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNk+M9QDwADhgGAWjR9awAAAABJRU5ErkJggg=='}, timeout=10)
    assert resp.status_code in [200], (
        f"Expected success, got {resp.status_code}: {resp.text[:200]}"
    )



def test_post__api_v2_match_images_semantic_image1_data_invalid_b64(base_url):
    """[rule:semantic_probe] 'image1_data' tag=base64_image probe=invalid_b64."""
    resp = requests.post(f"{base_url}/api/v2/match-images", json={'image1_data': 'not_base64!@#', 'image2_data': 'iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNk+M9QDwADhgGAWjR9awAAAABJRU5ErkJggg=='}, timeout=10)
    assert resp.status_code < 500, (
        f"[FAIL] semantic:invalid_b64 on 'image1_data' — server crashed\n"
        f"  Status : {resp.status_code}\n"
        f"  Body   : {resp.text[:300]}"
    )
    body_json = resp.json()
    assert body_json.get("success") == False or body_json.get("error_code", 0) < 0, (
        f"[FAIL] semantic:invalid_b64 on 'image1_data' — expected error response\n"
        f"  success    : {body_json.get('success')}\n"
        f"  error_code : {body_json.get('error_code')}\n"
        f"  msg        : {body_json.get('msg')}\n"
        f"  Full body  : {resp.text[:300]}"
    )




def test_post__api_v2_match_images_semantic_image1_data_empty_b64(base_url):
    """[rule:semantic_probe] 'image1_data' tag=base64_image probe=empty_b64."""
    resp = requests.post(f"{base_url}/api/v2/match-images", json={'image1_data': '', 'image2_data': 'iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNk+M9QDwADhgGAWjR9awAAAABJRU5ErkJggg=='}, timeout=10)
    assert resp.status_code < 500, (
        f"[FAIL] semantic:empty_b64 on 'image1_data' — server crashed\n"
        f"  Status : {resp.status_code}\n"
        f"  Body   : {resp.text[:300]}"
    )
    body_json = resp.json()
    assert body_json.get("success") == False or body_json.get("error_code", 0) < 0, (
        f"[FAIL] semantic:empty_b64 on 'image1_data' — expected error response\n"
        f"  success    : {body_json.get('success')}\n"
        f"  error_code : {body_json.get('error_code')}\n"
        f"  msg        : {body_json.get('msg')}\n"
        f"  Full body  : {resp.text[:300]}"
    )




def test_post__api_v2_match_images_semantic_image2_data_invalid_b64(base_url):
    """[rule:semantic_probe] 'image2_data' tag=base64_image probe=invalid_b64."""
    resp = requests.post(f"{base_url}/api/v2/match-images", json={'image1_data': 'iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNk+M9QDwADhgGAWjR9awAAAABJRU5ErkJggg==', 'image2_data': 'not_base64!@#'}, timeout=10)
    assert resp.status_code < 500, (
        f"[FAIL] semantic:invalid_b64 on 'image2_data' — server crashed\n"
        f"  Status : {resp.status_code}\n"
        f"  Body   : {resp.text[:300]}"
    )
    body_json = resp.json()
    assert body_json.get("success") == False or body_json.get("error_code", 0) < 0, (
        f"[FAIL] semantic:invalid_b64 on 'image2_data' — expected error response\n"
        f"  success    : {body_json.get('success')}\n"
        f"  error_code : {body_json.get('error_code')}\n"
        f"  msg        : {body_json.get('msg')}\n"
        f"  Full body  : {resp.text[:300]}"
    )




def test_post__api_v2_match_images_semantic_image2_data_empty_b64(base_url):
    """[rule:semantic_probe] 'image2_data' tag=base64_image probe=empty_b64."""
    resp = requests.post(f"{base_url}/api/v2/match-images", json={'image1_data': 'iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNk+M9QDwADhgGAWjR9awAAAABJRU5ErkJggg==', 'image2_data': ''}, timeout=10)
    assert resp.status_code < 500, (
        f"[FAIL] semantic:empty_b64 on 'image2_data' — server crashed\n"
        f"  Status : {resp.status_code}\n"
        f"  Body   : {resp.text[:300]}"
    )
    body_json = resp.json()
    assert body_json.get("success") == False or body_json.get("error_code", 0) < 0, (
        f"[FAIL] semantic:empty_b64 on 'image2_data' — expected error response\n"
        f"  success    : {body_json.get('success')}\n"
        f"  error_code : {body_json.get('error_code')}\n"
        f"  msg        : {body_json.get('msg')}\n"
        f"  Full body  : {resp.text[:300]}"
    )

